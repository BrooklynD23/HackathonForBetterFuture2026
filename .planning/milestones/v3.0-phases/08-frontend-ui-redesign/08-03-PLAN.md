---
phase: 08-frontend-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"
autonomous: true
requirements: [REQ-UI-02]
must_haves:
  truths:
    - "Landing page renders as a single full-page HTML document via st.components.v1.html()"
    - "Landing page hides all Streamlit chrome (sidebar, header, footer)"
    - "Hero section shows 'Match your specialist database with every university opportunity'"
    - "Product preview bento shows UCLA Career Fair 2026 and Travis Miller with 94% match score"
    - "Features grid shows 3 columns: Web Scraping, Specialist CRM, Bridge Matching"
    - "6-factor MATCH_SCORE donut chart renders as SVG with segmented ring"
    - "Partner showcase shows CPP, UCLA, SDSU, UC DAVIS, USC, PORTLAND STATE"
    - "Sign in button and View Demo CTA are wired (using JS postMessage or href)"
    - "Real specialist name (Travis Miller, President, PureSpectrum) comes from data_helpers"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"
      provides: "Full landing page HTML renderer"
      exports: ["render_landing_page_v2"]
      min_lines: 200
  key_links:
    - from: "src/ui/landing_page_v2.py"
      to: "src/ui/html_base.py"
      via: "render_html_page()"
      pattern: "from src.ui.html_base import"
    - from: "src/ui/landing_page_v2.py"
      to: "src/ui/data_helpers.py"
      via: "load_specialists()"
      pattern: "from src.ui.data_helpers import"
---

<objective>
Build the pixel-faithful landing page that matches the `ia_smartmatch_landing_page_updated/code.html` mockup. This is the public-facing entry point — a full-page HTML experience rendered via `st.components.v1.html()` with Streamlit chrome hidden.

Purpose: Replace the current `landing_page.py` (which uses st.markdown HTML fragments) with a true pixel-faithful reproduction using the mockup HTML directly, populated with real data.

Output: `src/ui/landing_page_v2.py` with a `render_landing_page_v2()` function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-ui-redesign/08-CONTEXT.md
@Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/ia_smartmatch_landing_page_updated/code.html (PRIMARY SOURCE — reproduce this pixel-for-pixel)
@Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/academic_blueprints/DESIGN.md
@Category 3 - IA West Smart Match CRM/src/ui/landing_page.py (current v1 implementation for reference)

<interfaces>
From src/ui/html_base.py (created in Plan 01):
```python
def wrap_html(body: str, *, title: str = "IA SmartMatch", height: int = 2000, hide_chrome: bool = False) -> None: ...
def render_html_page(body: str, *, title: str = "IA SmartMatch", height: int = 4000, hide_chrome: bool = False) -> None: ...
```

From src/ui/data_helpers.py (created in Plan 02):
```python
def load_specialists() -> list[dict[str, str]]: ...
def get_initials(name: str) -> str: ...
```

From src/ui/design_system.py (created in Plan 01):
```python
TAILWIND_CONFIG: str  # Full tailwind config script tag
SHARED_CSS: str       # hero-gradient, glass-panel, etc.
FONT_LINKS: str       # Google Fonts links
TAILWIND_CDN: str     # CDN script tag
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing_page_v2.py — full HTML landing page</name>
  <files>Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/ia_smartmatch_landing_page_updated/code.html (THE source of truth — reproduce all sections)
    - Category 3 - IA West Smart Match CRM/src/ui/html_base.py (wrap_html / render_html_page API)
    - Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py (load_specialists for real data)
    - Category 3 - IA West Smart Match CRM/src/ui/design_system.py (HIDE_STREAMLIT_CHROME)
  </read_first>
  <action>
Create `src/ui/landing_page_v2.py` with a single public function:

```python
def render_landing_page_v2() -> None:
```

This function:
1. Calls `load_specialists()` to get real specialist data. Finds "Travis Miller" (index 0) to populate the product preview bento with his real data: name="Travis Miller", title="SVP, Sales & Client Development", company="PureSpectrum", initials="TM".
2. Builds the full HTML body string by reproducing the mockup HTML structure. The body content goes from the `<nav>` through `<footer>` — **copy the mockup HTML nearly verbatim**, but:
   - Replace the hardcoded "SVP Sales, TechCorp" with the real title and company from data: `f"{specialist['title']}, {specialist['company']}"`
   - The "Sign in" nav button should use: `onclick="window.parent.postMessage({type: 'streamlit:setComponentValue', value: 'login'}, '*')"` (or simpler: just leave as non-functional `href="#"` since this is a demo landing page)
   - The "Start Matching" and "View Demo" hero CTAs: leave as non-functional buttons (`href="#"`) — navigation is handled by Streamlit buttons below the component
   - Keep all Tailwind classes, Material Symbols icons, and structural HTML exactly matching the mockup
   - Keep the SVG donut chart for 6-factor MATCH_SCORE (lines 275-286 of mockup) — copy verbatim
   - Keep the partner showcase: CPP, UCLA, SDSU, UC DAVIS, USC, PORTLAND STATE
   - Remove all external image `src` URLs (the googleusercontent URLs) — replace with placeholder colored divs or remove `<img>` tags. For the heatmap placeholder, use a colored div.
3. Calls `render_html_page(body_html, title="IA SmartMatch | Specialist-to-Opportunity AI Matching", height=6000, hide_chrome=True)`
4. After the html component, render two Streamlit buttons for navigation (these appear below the HTML component):
   ```python
   col1, col2, col3 = st.columns([1, 1, 1])
   with col1:
       if st.button("Sign In", type="primary", use_container_width=True):
           navigate_to("login")
   with col2:
       if st.button("View Demo", use_container_width=True):
           set_user_role("coordinator")
           navigate_to("coordinator")
   ```

Import `navigate_to`, `set_user_role` from `src.ui.page_router`.

CRITICAL: The HTML body must include ALL sections from the mockup:
- Glass nav bar (fixed top)
- Hero section with tagline
- Product preview bento (UCLA Career Fair 2026 + specialist recommendation + Bridge Logic panel)
- Features grid (3 columns)
- 6-factor MATCH_SCORE with SVG donut
- Automation pipeline visual
- Analytics dashboard (heatmap placeholder + matching funnel)
- Partner showcase
- Final CTA
- Footer

The HTML should be a single f-string or string with `.format()` calls for the dynamic specialist data. Use triple-quoted f-string for readability.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/DangT/Documents/GitHub/HackathonForBetterFuture2026" && python -c "
import src.ui.landing_page_v2 as lp
import inspect
src = inspect.getsource(lp)
assert 'render_landing_page_v2' in src
assert 'Travis Miller' in src or 'load_specialists' in src
assert 'UCLA Career Fair 2026' in src
assert 'Match your specialist database' in src
assert 'hero-gradient' in src
assert 'CPP' in src and 'UCLA' in src and 'SDSU' in src
assert 'render_html_page' in src or 'wrap_html' in src
assert 'hide_chrome' in src or 'HIDE_STREAMLIT_CHROME' in src
assert 'stroke-dasharray' in src  # SVG donut chart
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/landing_page_v2.py`
    - `grep -c "render_landing_page_v2" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - `grep -c "UCLA Career Fair 2026" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - `grep -c "Match your specialist database" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - `grep -c "stroke-dasharray" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1 (SVG donut)
    - `grep -c "render_html_page" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - `grep -c "load_specialists" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - `grep -c "PORTLAND STATE" "Category 3 - IA West Smart Match CRM/src/ui/landing_page_v2.py"` returns >= 1
    - Module imports without error: `python -c "from src.ui.landing_page_v2 import render_landing_page_v2"`
  </acceptance_criteria>
  <done>Landing page v2 renders a pixel-faithful reproduction of the mockup HTML via st.components.v1.html(). All sections present. Real specialist data (Travis Miller from PureSpectrum) populates the product preview. SVG donut chart for 6-factor MATCH_SCORE renders inline. Streamlit chrome is hidden.</done>
</task>

</tasks>

<verification>
- Landing page renders the full mockup HTML through st.components.v1.html()
- Travis Miller's real data from data_speaker_profiles.csv appears in the product preview
- SVG donut chart with stroke-dasharray segments renders the 6-factor breakdown
- All 6 partner universities appear in the showcase section
- Streamlit chrome (sidebar, header, footer) is hidden
</verification>

<success_criteria>
`render_landing_page_v2()` produces a full-page HTML rendering matching the mockup. The page includes all sections: nav, hero, product preview with real data, features grid, match score donut, automation pipeline, analytics, partners, CTA, footer.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-ui-redesign/08-03-SUMMARY.md`
</output>
