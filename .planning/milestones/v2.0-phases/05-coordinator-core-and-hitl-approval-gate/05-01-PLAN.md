---
phase: 05-coordinator-core-and-hitl-approval-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/coordinator/__init__.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/approval.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/intent_parser.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/suggestions.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_approval.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_intent_parser.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_suggestions.py"
autonomous: true
requirements:
  - HITL-01
  - HITL-02
  - HITL-03
  - ORCH-04

must_haves:
  truths:
    - "ActionProposal state machine enforces proposed->approved->executing->completed lifecycle"
    - "ActionProposal.reject() transitions from proposed to rejected only"
    - "ActionProposal.stub_execute() raises ValueError if status is not approved"
    - "parse_intent returns a ParsedIntent with one of the 5 supported intents"
    - "parse_intent returns intent=unknown on Gemini failure or malformed JSON"
    - "check_staleness_conditions returns a proactive suggestion when data is stale or empty"
    - "check_staleness_conditions returns empty list when data is fresh"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/approval.py"
      provides: "ActionProposal dataclass with state machine methods"
      exports: ["ActionProposal", "ProposalStatus"]
      contains: "def approve"
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/intent_parser.py"
      provides: "ParsedIntent dataclass and parse_intent function"
      exports: ["ParsedIntent", "parse_intent", "SUPPORTED_INTENTS", "ACTION_REGISTRY"]
      contains: "def parse_intent"
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/suggestions.py"
      provides: "Proactive staleness suggestions"
      exports: ["check_staleness_conditions", "STALENESS_HOURS"]
      contains: "def check_staleness_conditions"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_approval.py"
      provides: "State machine transition tests"
      contains: "class TestApprovalStateMachine"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_intent_parser.py"
      provides: "Intent parsing tests with mocked Gemini"
      contains: "class TestParseIntent"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_suggestions.py"
      provides: "Staleness condition tests"
      contains: "class TestStalenessCheck"
  key_links:
    - from: "src/coordinator/intent_parser.py"
      to: "src/gemini_client.py"
      via: "generate_text() call for structured JSON"
      pattern: "from src\\.gemini_client import generate_text"
    - from: "src/coordinator/suggestions.py"
      to: "src/coordinator/approval.py"
      via: "returns ActionProposal instances"
      pattern: "from src\\.coordinator\\.approval import ActionProposal"
---

<objective>
Create the three pure-Python coordinator modules that power Phase 5: the ActionProposal state machine (approval.py), the Gemini intent parser (intent_parser.py), and the proactive staleness suggestion engine (suggestions.py). All modules are pure Python with zero Streamlit imports, fully testable in isolation.

Purpose: These modules are the core logic layer for the HITL approval gate. Building and testing them first (before UI integration in Plan 02) ensures the state machine is proven correct and the intent parser contract is stable before any Streamlit wiring.

Output: 3 source modules under src/coordinator/, 3 test files, all tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-coordinator-core-and-hitl-approval-gate/05-CONTEXT.md
@.planning/phases/05-coordinator-core-and-hitl-approval-gate/05-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/gemini_client.py:
```python
class GeminiAPIError(RuntimeError):
    def __init__(self, message: str, *, status_code: int | None = None, retryable: bool = False) -> None: ...

def generate_text(
    messages: list[dict[str, str]],
    *,
    api_key: str,
    model: str,
    system_instruction: str = "",
    temperature: float = 0.7,
    max_output_tokens: int = 2000,
    timeout: float = 30.0,
) -> str: ...
```

From src/config.py:
```python
GEMINI_API_KEY: str  # from env or Streamlit secrets
GEMINI_TEXT_MODEL: str  # default "gemini-2.5-flash-lite"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionProposal state machine + tests (approval.py)</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/coordinator/__init__.py
    Category 3 - IA West Smart Match CRM/src/coordinator/approval.py
    Category 3 - IA West Smart Match CRM/tests/test_approval.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/config.py
    Category 3 - IA West Smart Match CRM/tests/conftest.py
    .planning/phases/05-coordinator-core-and-hitl-approval-gate/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: ActionProposal() creates with status="proposed" and a valid UUID id
    - Test: proposal.approve() transitions status from "proposed" to "approved"
    - Test: proposal.reject() transitions status from "proposed" to "rejected"
    - Test: proposal.approve() raises ValueError when status is not "proposed"
    - Test: proposal.reject() raises ValueError when status is not "proposed"
    - Test: proposal.stub_execute() transitions "approved" -> "executing" -> "completed" with result string
    - Test: proposal.stub_execute() raises ValueError when status is not "approved"
    - Test: proposal.stub_execute() sets result to "[Stub] {agent} completed successfully."
    - Test: ActionProposal can store edited params (modify params dict before approve, then approve)
    - Test: ActionProposal.source defaults to "parsed" and can be set to "proactive"
  </behavior>
  <action>
    Create `src/coordinator/__init__.py` (empty, makes it a package).

    Create `src/coordinator/approval.py` with:
    - `ProposalStatus = Literal["proposed", "approved", "executing", "completed", "failed", "rejected"]`
    - `@dataclass class ActionProposal` with fields:
      - `id: str` (default_factory: `lambda: str(uuid.uuid4())`)
      - `intent: str = ""`
      - `agent: str = ""`
      - `description: str = ""`
      - `reasoning: str = ""`
      - `params: dict[str, Any] = field(default_factory=dict)`
      - `status: ProposalStatus = "proposed"`
      - `source: Literal["parsed", "proactive"] = "parsed"`
      - `created_at: str` (default_factory: `lambda: datetime.datetime.now().strftime("%H:%M:%S")`)
      - `result: str | None = None`
    - Method `approve(self) -> None`: guard `status != "proposed"` -> raise ValueError; set `status = "approved"`
    - Method `reject(self) -> None`: guard `status != "proposed"` -> raise ValueError; set `status = "rejected"`
    - Method `stub_execute(self) -> None`: guard `status != "approved"` -> raise ValueError; set `status = "executing"`, then `result = f"[Stub] {self.agent} completed successfully."`, then `status = "completed"`

    Create `tests/test_approval.py` with class `TestApprovalStateMachine` containing the tests listed in <behavior>. Use `import pytest` and `from src.coordinator.approval import ActionProposal, ProposalStatus`. No Streamlit imports needed.

    Per HITL-02: approve/reject/edit params are all tested. Per HITL-03: stub_execute raises if not approved (no execution without approval).
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && python -m pytest tests/test_approval.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/coordinator/__init__.py exists (can be empty)
    - src/coordinator/approval.py contains `class ActionProposal`
    - src/coordinator/approval.py contains `def approve(self)`
    - src/coordinator/approval.py contains `def reject(self)`
    - src/coordinator/approval.py contains `def stub_execute(self)`
    - src/coordinator/approval.py contains `ProposalStatus = Literal`
    - tests/test_approval.py contains `class TestApprovalStateMachine`
    - tests/test_approval.py exits 0 with all tests passing
    - No `import streamlit` in approval.py
  </acceptance_criteria>
  <done>ActionProposal state machine with approve/reject/stub_execute methods is fully tested. All transitions guarded. No Streamlit dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Intent parser + tests (intent_parser.py)</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/coordinator/intent_parser.py
    Category 3 - IA West Smart Match CRM/tests/test_intent_parser.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/gemini_client.py
    Category 3 - IA West Smart Match CRM/src/config.py
    Category 3 - IA West Smart Match CRM/tests/conftest.py
    Category 3 - IA West Smart Match CRM/src/coordinator/approval.py
  </read_first>
  <behavior>
    - Test: parse_intent("find events at UCLA") returns ParsedIntent with intent="discover_events" (mock Gemini to return valid JSON)
    - Test: parse_intent returns intent="unknown" when Gemini returns malformed JSON (mock generate_text to return "not json")
    - Test: parse_intent returns intent="unknown" when Gemini raises GeminiAPIError (mock generate_text to raise)
    - Test: parse_intent returns intent="unknown" when Gemini returns an unsupported intent value
    - Test: parse_intent preserves raw_text in the returned ParsedIntent
    - Test: parse_intent strips markdown fences from Gemini response before JSON parsing (mock returns ```json\n{...}\n```)
    - Test: ParsedIntent is frozen (immutable)
    - Test: SUPPORTED_INTENTS contains exactly {"discover_events", "rank_speakers", "generate_outreach", "check_contacts", "unknown"}
    - Test: ACTION_REGISTRY has 4 entries with correct intent/agent/description fields
  </behavior>
  <action>
    Create `src/coordinator/intent_parser.py` with:

    - `SUPPORTED_INTENTS = frozenset({"discover_events", "rank_speakers", "generate_outreach", "check_contacts", "unknown"})`

    - `@dataclass(frozen=True) class ParsedIntent` with fields:
      - `intent: str` (one of SUPPORTED_INTENTS)
      - `agent: str` (display name like "Discovery Agent")
      - `params: dict[str, Any]`
      - `reasoning: str`
      - `raw_text: str` (original user text)

    - `ACTION_REGISTRY` list of 4 dicts:
      - `{"intent": "discover_events", "agent": "Discovery Agent", "description": "Scrape universities for new events"}`
      - `{"intent": "rank_speakers", "agent": "Matching Agent", "description": "Rank speakers for a target event"}`
      - `{"intent": "generate_outreach", "agent": "Outreach Agent", "description": "Draft outreach emails for a match"}`
      - `{"intent": "check_contacts", "agent": "Contacts Agent", "description": "Review POC contact status"}`

    - `_SYSTEM_PROMPT` string template (see 05-RESEARCH.md Pattern 3 for exact text) with `{actions}` placeholder

    - `_strip_markdown_fence(text: str) -> str` helper that removes triple-backtick fences

    - `def parse_intent(text: str) -> ParsedIntent` that:
      1. Formats system prompt with ACTION_REGISTRY as JSON
      2. Calls `generate_text(messages=[{"role": "user", "content": text}], api_key=GEMINI_API_KEY, model=GEMINI_TEXT_MODEL, system_instruction=system, temperature=0.1, max_output_tokens=300, timeout=10.0)`
      3. Strips markdown fences from response
      4. Parses JSON with `json.loads()`
      5. Validates intent is in SUPPORTED_INTENTS (default to "unknown")
      6. Returns `ParsedIntent(intent=..., agent=..., params=..., reasoning=..., raw_text=text)`
      7. On ANY exception (GeminiAPIError, JSONDecodeError, KeyError, TypeError), returns `ParsedIntent(intent="unknown", agent="Jarvis", params={}, reasoning="", raw_text=text)`

    Create `tests/test_intent_parser.py` with class `TestParseIntent`. Mock `src.coordinator.intent_parser.generate_text` using `@patch("src.coordinator.intent_parser.generate_text")` to control Gemini responses. No live API calls.

    Per HITL-01: ParsedIntent carries agent name, description (via ACTION_REGISTRY lookup), and reasoning.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && python -m pytest tests/test_intent_parser.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/coordinator/intent_parser.py contains `class ParsedIntent`
    - src/coordinator/intent_parser.py contains `def parse_intent(text: str) -> ParsedIntent`
    - src/coordinator/intent_parser.py contains `SUPPORTED_INTENTS`
    - src/coordinator/intent_parser.py contains `ACTION_REGISTRY`
    - src/coordinator/intent_parser.py contains `def _strip_markdown_fence`
    - src/coordinator/intent_parser.py contains `from src.gemini_client import generate_text`
    - tests/test_intent_parser.py contains `class TestParseIntent`
    - tests/test_intent_parser.py exits 0 with all tests passing
    - No `import streamlit` in intent_parser.py
  </acceptance_criteria>
  <done>Intent parser returns structured ParsedIntent from Gemini JSON. Gracefully falls back to intent="unknown" on any failure. Markdown fence stripping handles Gemini formatting quirks. All tests green with mocked Gemini.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Proactive suggestions engine + tests (suggestions.py)</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/coordinator/suggestions.py
    Category 3 - IA West Smart Match CRM/tests/test_suggestions.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/coordinator/approval.py
    Category 3 - IA West Smart Match CRM/tests/conftest.py
    .planning/phases/05-coordinator-core-and-hitl-approval-gate/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: check_staleness_conditions returns 1 suggestion when scraped_events is empty list and scraped_at is None
    - Test: check_staleness_conditions returns 1 suggestion when scraped_at is 25 hours ago
    - Test: check_staleness_conditions returns empty list when scraped_at is 1 hour ago and scraped_events is non-empty
    - Test: returned suggestion has intent="discover_events", agent="Discovery Agent", source="proactive"
    - Test: returned suggestion has status="proposed"
    - Test: _is_stale returns True for None timestamp
    - Test: _is_stale returns True for invalid timestamp string
    - Test: _is_stale returns False for timestamp 1 hour ago (within 24h threshold)
    - Test: STALENESS_HOURS equals 24
  </behavior>
  <action>
    Create `src/coordinator/suggestions.py` with:

    - `from src.coordinator.approval import ActionProposal`
    - `STALENESS_HOURS: int = 24`

    - `def check_staleness_conditions(scraped_events: list, scraped_at: str | None) -> list[ActionProposal]`:
      Pure function. If `scraped_events` is empty or `_is_stale(scraped_at, hours=STALENESS_HOURS)`, return a list containing one `ActionProposal(intent="discover_events", agent="Discovery Agent", description="Re-run event discovery scraper", reasoning="Discovery data is stale or empty -- re-running will surface new events.", source="proactive")`. Otherwise return empty list.

    - `def _is_stale(timestamp_str: str | None, hours: int) -> bool`:
      Returns True if `timestamp_str` is None, empty, or cannot be parsed with `datetime.datetime.fromisoformat()`, or if the parsed timestamp is more than `hours * 3600` seconds ago. Returns False otherwise.

    Create `tests/test_suggestions.py` with class `TestStalenessCheck`. Use `datetime.datetime` to generate timestamps for testing. Import directly from `src.coordinator.suggestions`. No Streamlit imports.

    Per ORCH-04: Jarvis proactively suggests actions when data is stale.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && python -m pytest tests/test_suggestions.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/coordinator/suggestions.py contains `def check_staleness_conditions`
    - src/coordinator/suggestions.py contains `def _is_stale`
    - src/coordinator/suggestions.py contains `STALENESS_HOURS = 24`
    - src/coordinator/suggestions.py contains `from src.coordinator.approval import ActionProposal`
    - tests/test_suggestions.py contains `class TestStalenessCheck`
    - tests/test_suggestions.py exits 0 with all tests passing
    - No `import streamlit` in suggestions.py
  </acceptance_criteria>
  <done>Staleness check returns proactive ActionProposal suggestions when data is stale/empty, returns empty list when fresh. All tests green. Pure Python, no Streamlit dependency.</done>
</task>

</tasks>

<verification>
Run all three new test files together:
```bash
cd "Category 3 - IA West Smart Match CRM" && python -m pytest tests/test_approval.py tests/test_intent_parser.py tests/test_suggestions.py -q --tb=short
```

Verify no Streamlit imports in coordinator modules:
```bash
grep -r "import streamlit" "Category 3 - IA West Smart Match CRM/src/coordinator/"
```
Should return no matches.
</verification>

<success_criteria>
- All 3 coordinator modules exist under src/coordinator/
- All 3 test files pass with zero failures
- No Streamlit imports in any coordinator module
- ActionProposal state machine enforces valid transitions with ValueError on invalid ones
- parse_intent gracefully handles all failure modes (network, JSON, unknown intent)
- check_staleness_conditions is a pure function returning ActionProposal instances
</success_criteria>

<output>
After completion, create `.planning/phases/05-coordinator-core-and-hitl-approval-gate/05-01-SUMMARY.md`
</output>
