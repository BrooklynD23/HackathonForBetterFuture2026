---
phase: 06-agent-tool-wrappers-and-result-bus
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/coordinator/tools/__init__.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/tools/discovery_tool.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/tools/matching_tool.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/tools/outreach_tool.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/tools/contacts_tool.py"
  - "Category 3 - IA West Smart Match CRM/src/coordinator/result_bus.py"
  - "Category 3 - IA West Smart Match CRM/data/poc_contacts.json"
  - "Category 3 - IA West Smart Match CRM/tests/test_discovery_tool.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_matching_tool.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_outreach_tool.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_contacts_tool.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_result_bus.py"
  - "Category 3 - IA West Smart Match CRM/tests/conftest.py"
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - POC-01
  - POC-02

must_haves:
  truths:
    - "Discovery tool wrapper calls scrape_university() and returns structured event list"
    - "Matching tool wrapper calls rank_speakers_for_event() and returns ranked speaker list"
    - "Outreach tool wrapper calls generate_outreach_email() and returns email content"
    - "Contacts tool loads poc_contacts.json and identifies overdue follow-ups"
    - "Result bus dispatches tool functions in daemon threads with per-proposal queues"
    - "Two parallel dispatches execute independently without corrupting each other"
    - "TOOL_REGISTRY maps all four intent names to their tool run() functions"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/tools/__init__.py"
      provides: "TOOL_REGISTRY dict mapping intent names to tool callables"
      contains: "TOOL_REGISTRY"
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/tools/discovery_tool.py"
      provides: "Thin adapter wrapping scrape_university()"
      exports: ["run", "TOOL_NAME"]
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/tools/matching_tool.py"
      provides: "Thin adapter wrapping rank_speakers_for_event()"
      exports: ["run", "TOOL_NAME"]
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/tools/outreach_tool.py"
      provides: "Thin adapter wrapping generate_outreach_email()"
      exports: ["run", "TOOL_NAME"]
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/tools/contacts_tool.py"
      provides: "POC contact queries with overdue detection"
      exports: ["run", "TOOL_NAME"]
    - path: "Category 3 - IA West Smart Match CRM/src/coordinator/result_bus.py"
      provides: "Thread dispatch and queue polling"
      exports: ["dispatch", "poll_results"]
    - path: "Category 3 - IA West Smart Match CRM/data/poc_contacts.json"
      provides: "Seed POC contact data"
      contains: "follow_up_due"
  key_links:
    - from: "src/coordinator/tools/discovery_tool.py"
      to: "src/scraping/scraper.py"
      via: "import scrape_university, UNIVERSITY_TARGETS"
      pattern: "from src\\.scraping\\.scraper import"
    - from: "src/coordinator/tools/matching_tool.py"
      to: "src/matching/engine.py"
      via: "import rank_speakers_for_event"
      pattern: "from src\\.matching\\.engine import"
    - from: "src/coordinator/tools/outreach_tool.py"
      to: "src/outreach/email_gen.py"
      via: "import generate_outreach_email"
      pattern: "from src\\.outreach\\.email_gen import"
    - from: "src/coordinator/tools/__init__.py"
      to: "all four tool modules"
      via: "TOOL_REGISTRY dict"
      pattern: "TOOL_REGISTRY.*dict"
    - from: "src/coordinator/result_bus.py"
      to: "st.session_state[result_queues]"
      via: "queue.Queue per proposal"
      pattern: "result_queues"
---

<objective>
Create the tool wrapper package, result bus, POC contact seed data, and all tests -- the pure-Python machinery that Phase 6 Plan 02 will wire into the Command Center UI.

Purpose: Deliver the core execution layer: four thin adapter tools wrapping existing SmartMatch services, a thread-based result bus for non-blocking dispatch, and POC contact data with overdue detection. All pure Python with zero Streamlit imports in tool modules (result_bus.py uses Streamlit session state only). This is the direct-dispatch fallback path -- no NemoClaw.

Output: 6 new source modules, 1 seed data file, 5 new test files, 1 updated conftest.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tool-wrappers-and-result-bus/06-CONTEXT.md
@.planning/phases/06-agent-tool-wrappers-and-result-bus/06-RESEARCH.md
@.planning/phases/05-coordinator-core-and-hitl-approval-gate/05-01-SUMMARY.md
@.planning/phases/05-coordinator-core-and-hitl-approval-gate/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/coordinator/approval.py:
```python
ProposalStatus = Literal["proposed", "approved", "executing", "completed", "failed", "rejected"]

@dataclass
class ActionProposal:
    id: str = field(default_factory=lambda: str(uuid.uuid4()))
    intent: str = ""
    agent: str = ""
    description: str = ""
    reasoning: str = ""
    params: dict[str, Any] = field(default_factory=dict)
    status: ProposalStatus = "proposed"
    source: Literal["parsed", "proactive"] = "parsed"
    created_at: str = field(default_factory=lambda: datetime.datetime.now().strftime("%H:%M:%S"))
    result: str | None = None

    def approve(self) -> None: ...
    def reject(self) -> None: ...
    def stub_execute(self) -> None: ...
```

From src/coordinator/intent_parser.py:
```python
SUPPORTED_INTENTS: frozenset[str] = frozenset({
    "discover_events", "rank_speakers", "generate_outreach", "check_contacts", "unknown",
})
ACTION_REGISTRY: list[dict[str, str]] = [
    {"intent": "discover_events",   "agent": "Discovery Agent",  ...},
    {"intent": "rank_speakers",     "agent": "Matching Agent",   ...},
    {"intent": "generate_outreach", "agent": "Outreach Agent",   ...},
    {"intent": "check_contacts",    "agent": "Contacts Agent",   ...},
]
```

From src/scraping/scraper.py (line 377):
```python
def scrape_university(
    url: str,
    method: Literal["bs4", "playwright"] = "bs4",
    cache_dir: str = DEFAULT_CACHE_DIR,
) -> dict[str, Any]: ...
```

From src/matching/engine.py (line 179):
```python
def rank_speakers_for_event(
    event_row: pd.Series,
    speakers_df: pd.DataFrame,
    speaker_embeddings: dict[str, np.ndarray],
    event_embedding: np.ndarray,
    ia_event_calendar: pd.DataFrame,
    top_n: int = 10,
    weights: Optional[dict[str, float]] = None,
    conversion_overrides: Optional[dict[str, float]] = None,
    student_interest_override: Optional[float] = None,
) -> list[dict[str, Any]]: ...
```

From src/outreach/email_gen.py (line 229):
```python
def generate_outreach_email(
    speaker: dict[str, Any],
    event: dict[str, Any],
    match_scores: dict[str, Any],
    model: str = EMAIL_MODEL,
    temperature: float = EMAIL_TEMPERATURE,
) -> dict[str, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tool wrappers, TOOL_REGISTRY, POC seed data, and all tool tests</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/coordinator/tools/__init__.py
    Category 3 - IA West Smart Match CRM/src/coordinator/tools/discovery_tool.py
    Category 3 - IA West Smart Match CRM/src/coordinator/tools/matching_tool.py
    Category 3 - IA West Smart Match CRM/src/coordinator/tools/outreach_tool.py
    Category 3 - IA West Smart Match CRM/src/coordinator/tools/contacts_tool.py
    Category 3 - IA West Smart Match CRM/data/poc_contacts.json
    Category 3 - IA West Smart Match CRM/tests/test_discovery_tool.py
    Category 3 - IA West Smart Match CRM/tests/test_matching_tool.py
    Category 3 - IA West Smart Match CRM/tests/test_outreach_tool.py
    Category 3 - IA West Smart Match CRM/tests/test_contacts_tool.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/scraping/scraper.py (lines 370-400 for scrape_university signature and UNIVERSITY_TARGETS dict)
    Category 3 - IA West Smart Match CRM/src/matching/engine.py (lines 170-210 for rank_speakers_for_event signature)
    Category 3 - IA West Smart Match CRM/src/outreach/email_gen.py (lines 220-250 for generate_outreach_email signature)
    Category 3 - IA West Smart Match CRM/src/coordinator/intent_parser.py (for SUPPORTED_INTENTS and ACTION_REGISTRY)
    Category 3 - IA West Smart Match CRM/src/coordinator/approval.py (for ActionProposal dataclass)
    Category 3 - IA West Smart Match CRM/data/ (list directory to see existing data files)
  </read_first>
  <behavior>
    - discovery_tool.run({"university": "UCLA"}) calls scrape_university() with correct url/method and returns {"status": "ok", "events": [...], "source": "cache"|"live"}
    - discovery_tool.run({}) uses first UNIVERSITY_TARGETS key as default
    - discovery_tool.run() propagates exceptions from scrape_university() (result_bus thread catches these)
    - matching_tool.run({"event_row": ..., "speakers_df": ..., "speaker_embeddings": ..., "event_embedding": ..., "ia_event_calendar": ...}) calls rank_speakers_for_event() and returns {"status": "ok", "rankings": [...]}
    - matching_tool.run({}) with missing required keys returns {"status": "error", "error": "Missing required param: event_row"}
    - outreach_tool.run({"speaker": {...}, "event": {...}, "match_scores": {...}}) calls generate_outreach_email() and returns {"status": "ok", "email": {...}}
    - outreach_tool.run({}) with missing keys returns {"status": "error", "error": "Missing required param: speaker"}
    - contacts_tool.run({}) loads poc_contacts.json, returns {"status": "ok", "contacts": [...], "overdue": [...], "total": N, "overdue_count": M}
    - contacts_tool.run({}) identifies contacts where follow_up_due < today as overdue
    - contacts_tool.run({}) returns empty lists gracefully if poc_contacts.json missing
    - TOOL_REGISTRY maps "discover_events" -> discovery_tool.run, "rank_speakers" -> matching_tool.run, "generate_outreach" -> outreach_tool.run, "check_contacts" -> contacts_tool.run
  </behavior>
  <action>
    Create `src/coordinator/tools/` package with 5 files:

    1. **`discovery_tool.py`**: Import `scrape_university` and `UNIVERSITY_TARGETS` from `src.scraping.scraper`. Define `TOOL_NAME = "discover_events"`. Function `run(params: dict[str, Any]) -> dict[str, Any]` extracts `params.get("university")`, looks up target in UNIVERSITY_TARGETS (default to first key), calls `scrape_university(url=target["url"], method=target["method"])`, returns `{"status": "ok", "events": result.get("events", []), "source": result.get("source", "live")}`. No Streamlit imports. No try/except -- let exceptions propagate for result_bus to catch.

    2. **`matching_tool.py`**: Import `rank_speakers_for_event` from `src.matching.engine`. Define `TOOL_NAME = "rank_speakers"`. Function `run(params: dict[str, Any]) -> dict[str, Any]` extracts required keys: `event_row`, `speakers_df`, `speaker_embeddings`, `event_embedding`, `ia_event_calendar`. If any required key missing, return `{"status": "error", "error": f"Missing required param: {key}"}`. Optional keys: `top_n` (default 10), `weights`, `conversion_overrides`, `student_interest_override`. Call `rank_speakers_for_event(...)` with extracted args, return `{"status": "ok", "rankings": result}`. No Streamlit imports.

    3. **`outreach_tool.py`**: Import `generate_outreach_email` from `src.outreach.email_gen`. Define `TOOL_NAME = "generate_outreach"`. Function `run(params: dict[str, Any]) -> dict[str, Any]` extracts required keys: `speaker`, `event`, `match_scores`. If any missing, return error dict. Call `generate_outreach_email(speaker=speaker, event=event, match_scores=match_scores)`, return `{"status": "ok", "email": result}`. No Streamlit imports.

    4. **`contacts_tool.py`**: No external service imports. Define `TOOL_NAME = "check_contacts"`. Define `_CONTACTS_PATH = Path(__file__).resolve().parents[3] / "data" / "poc_contacts.json"`. Function `_load_contacts() -> list[dict[str, Any]]` reads and parses the JSON file, returns `[]` on FileNotFoundError or JSONDecodeError. Function `run(params: dict[str, Any]) -> dict[str, Any]` calls `_load_contacts()`, computes `overdue = [c for c in contacts if c.get("follow_up_due") and datetime.date.fromisoformat(c["follow_up_due"]) < datetime.date.today()]`, returns `{"status": "ok", "contacts": contacts, "overdue": overdue, "total": len(contacts), "overdue_count": len(overdue)}`. No Streamlit imports.

    5. **`__init__.py`**: Import `run` from each tool module as `_discovery_run`, `_matching_run`, `_outreach_run`, `_contacts_run`. Define `TOOL_REGISTRY: dict[str, callable] = {"discover_events": _discovery_run, "rank_speakers": _matching_run, "generate_outreach": _outreach_run, "check_contacts": _contacts_run}`.

    6. **`data/poc_contacts.json`**: Create seed data with 5 contacts. At least 2 must have `follow_up_due` dates before 2026-03-24 (overdue for demo). Schema per CONTEXT.md: `{name, email, org, role, comm_history: [{date, type, summary}], last_contact, follow_up_due}`. Use CPP-related names/orgs (e.g., Cal Poly Pomona Career Center, UCLA Engineering, CSUF Business School).

    Write tests FIRST (RED), then implement (GREEN):

    - `tests/test_discovery_tool.py`: Mock `scrape_university` via `@patch("src.coordinator.tools.discovery_tool.scrape_university")`. Test run returns events, test default university fallback, test exception propagation.
    - `tests/test_matching_tool.py`: Mock `rank_speakers_for_event`. Test run returns rankings, test missing required params returns error dict.
    - `tests/test_outreach_tool.py`: Mock `generate_outreach_email`. Test run returns email dict, test missing required params returns error dict.
    - `tests/test_contacts_tool.py`: Mock `_load_contacts` or use a tmp_path fixture with a test JSON file. Test run returns all contacts + overdue subset, test overdue detection with past dates, test empty/missing file returns graceful empty.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && .venv/bin/python -m pytest tests/test_discovery_tool.py tests/test_matching_tool.py tests/test_outreach_tool.py tests/test_contacts_tool.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/coordinator/tools/__init__.py contains `TOOL_REGISTRY` with keys "discover_events", "rank_speakers", "generate_outreach", "check_contacts"
    - src/coordinator/tools/discovery_tool.py contains `def run(params: dict` and `from src.scraping.scraper import`
    - src/coordinator/tools/matching_tool.py contains `def run(params: dict` and `from src.matching.engine import`
    - src/coordinator/tools/outreach_tool.py contains `def run(params: dict` and `from src.outreach.email_gen import`
    - src/coordinator/tools/contacts_tool.py contains `def run(params: dict` and `follow_up_due` and `datetime.date.fromisoformat`
    - data/poc_contacts.json is valid JSON array with at least 5 objects each containing "follow_up_due" key
    - None of the four tool modules contain `import streamlit`
    - All four test files pass with 0 failures
  </acceptance_criteria>
  <done>All four tool wrappers implement run(params) -> dict, TOOL_REGISTRY maps all four intents, poc_contacts.json has 5+ seed contacts with 2+ overdue, all tool tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create result_bus.py with dispatch/poll and tests, update conftest.py with st.fragment mock</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/coordinator/result_bus.py
    Category 3 - IA West Smart Match CRM/tests/test_result_bus.py
    Category 3 - IA West Smart Match CRM/tests/conftest.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/tests/conftest.py (current state -- need to add st.fragment mock)
    Category 3 - IA West Smart Match CRM/src/coordinator/approval.py (ActionProposal dataclass for reference)
    Category 3 - IA West Smart Match CRM/src/runtime_state.py (session state initialization pattern)
  </read_first>
  <behavior>
    - dispatch("pid-1", fast_tool, {}) spawns daemon thread, tool result appears in st.session_state["result_queues"]["pid-1"] queue
    - dispatch creates a fresh queue.Queue(maxsize=1) unconditionally for each proposal_id
    - poll_results() returns list of (proposal_id, payload) for queues with available results
    - poll_results() uses get_nowait() and returns [] when all queues are empty
    - dispatch with failing tool posts {"status": "failed", "error": "boom"} to queue
    - dispatch with succeeding tool posts {"status": "completed", "result": {...}} to queue
    - Two parallel dispatches produce independent results that do not cross-contaminate
    - conftest.py st.fragment mock is a no-op decorator: lambda **kw: (lambda f: f)
  </behavior>
  <action>
    **First: Update conftest.py** -- Add `st.fragment` mock BEFORE any new code imports it.

    In `tests/conftest.py`, after the line `_mock_st.chat_message = MagicMock()` (line 63) and before `sys.modules["streamlit"] = _mock_st` (line 65), add:
    ```python
    _mock_st.fragment = lambda **kw: (lambda f: f)  # type: ignore[attr-defined]
    ```
    This makes `@st.fragment(run_every=2)` a transparent identity decorator in tests.

    **Second: Create `src/coordinator/result_bus.py`** with two public functions:

    ```python
    def dispatch(
        proposal_id: str,
        tool_fn: Callable[[dict[str, Any]], dict[str, Any]],
        params: dict[str, Any],
    ) -> None:
    ```
    - Access `st.session_state.setdefault("result_queues", {})`
    - Create `q = queue.Queue(maxsize=1)` -- always fresh, unconditionally overwrite `result_queues[proposal_id]`
    - Define inner `_worker()` that calls `tool_fn(params)`, puts `{"status": "completed", "result": result}` on queue, or catches Exception and puts `{"status": "failed", "error": str(exc)}`
    - Spawn `threading.Thread(target=_worker, name=f"tool-{proposal_id[:8]}", daemon=True)` and start it

    ```python
    def poll_results() -> list[tuple[str, dict[str, Any]]]:
    ```
    - Read `st.session_state.get("result_queues", {})`
    - Iterate over items, try `q.get_nowait()`, catch `queue.Empty`, collect ready results as `(pid, payload)` tuples
    - Return list of ready results

    **Third: Create tests** (write BEFORE implementation):

    `tests/test_result_bus.py`:
    - `setup_method`: set `st.session_state["result_queues"] = {}`
    - `test_dispatch_posts_completed_result`: dispatch with `lambda params: {"events": ["A"]}`, sleep 0.1s, poll_results returns 1 result with status "completed"
    - `test_dispatch_posts_failed_on_exception`: dispatch with raising lambda, poll returns status "failed" with error string
    - `test_two_parallel_tasks_are_independent`: dispatch two tasks with different params, sleep 0.2s, poll returns both results, each with correct val
    - `test_poll_results_returns_empty_when_no_results`: poll with empty result_queues returns []
    - `test_dispatch_creates_fresh_queue`: dispatch same proposal_id twice, second dispatch creates new queue (not reusing old)
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && .venv/bin/python -m pytest tests/test_result_bus.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/coordinator/result_bus.py contains `def dispatch(` and `def poll_results(`
    - src/coordinator/result_bus.py contains `queue.Queue(maxsize=1)` and `threading.Thread` and `daemon=True`
    - src/coordinator/result_bus.py contains `get_nowait()` and `queue.Empty`
    - src/coordinator/result_bus.py contains `result_queues` (session state key)
    - tests/conftest.py contains `_mock_st.fragment = lambda`
    - tests/test_result_bus.py contains `test_two_parallel_tasks_are_independent`
    - All result_bus tests pass with 0 failures
    - Full test suite still passes: `.venv/bin/python -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>result_bus.py dispatches daemon threads with per-proposal queues, poll_results drains non-blocking, parallel tasks proven independent, conftest has st.fragment mock, full suite green</done>
</task>

</tasks>

<verification>
1. All new tool wrapper tests pass: `.venv/bin/python -m pytest tests/test_discovery_tool.py tests/test_matching_tool.py tests/test_outreach_tool.py tests/test_contacts_tool.py tests/test_result_bus.py -x -v`
2. Full regression suite still passes: `.venv/bin/python -m pytest tests/ -x`
3. No tool module imports streamlit: `grep -r "import streamlit" "Category 3 - IA West Smart Match CRM/src/coordinator/tools/"` returns empty
4. TOOL_REGISTRY has 4 entries: `grep "TOOL_REGISTRY" "Category 3 - IA West Smart Match CRM/src/coordinator/tools/__init__.py"`
</verification>

<success_criteria>
- 4 tool wrappers (discovery, matching, outreach, contacts) each with `run(params) -> dict` calling existing service functions unchanged
- TOOL_REGISTRY maps all 4 intent names to tool callables
- result_bus.py dispatches daemon threads and polls results non-blocking
- poc_contacts.json has 5+ seed contacts with 2+ overdue for demo
- conftest.py has st.fragment mock
- All new tests green + full 501-test suite green
- Zero changes to existing function signatures in scraper.py, engine.py, email_gen.py
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tool-wrappers-and-result-bus/06-01-SUMMARY.md`
</output>
