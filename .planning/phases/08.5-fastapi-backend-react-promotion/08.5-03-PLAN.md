---
phase: 08.5-fastapi-backend-react-promotion
plan: 03
type: execute
wave: 2
depends_on: ["08.5-01", "08.5-02"]
files_modified:
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Volunteers.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Pipeline.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Dashboard.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Calendar.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Outreach.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Opportunities.tsx"
autonomous: false

requirements: []

must_haves:
  truths:
    - "Volunteers page renders a grid of real specialist profiles from /api/data/specialists"
    - "Pipeline page renders funnel chart with real pipeline data from /api/data/pipeline"
    - "Dashboard page shows real summary metrics derived from API data"
    - "AI Matching page can trigger /api/matching/rank and display ranked results"
    - "Calendar page renders events from /api/data/calendar"
    - "All pages gracefully handle loading states and API errors"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Volunteers.tsx"
      provides: "Volunteer grid with real specialist data"
      contains: "fetchSpecialists"
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Pipeline.tsx"
      provides: "Pipeline funnel with real data"
      contains: "fetchPipeline"
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx"
      provides: "Match ranking with API calls"
      contains: "rankSpeakers"
  key_links:
    - from: "frontend/src/app/pages/Volunteers.tsx"
      to: "frontend/src/lib/api.ts"
      via: "import fetchSpecialists"
      pattern: "import.*fetchSpecialists.*from.*lib/api"
    - from: "frontend/src/app/pages/Pipeline.tsx"
      to: "frontend/src/lib/api.ts"
      via: "import fetchPipeline"
      pattern: "import.*fetchPipeline.*from.*lib/api"
    - from: "frontend/src/app/pages/AIMatching.tsx"
      to: "frontend/src/lib/api.ts"
      via: "import rankSpeakers"
      pattern: "import.*rankSpeakers.*from.*lib/api"
---

<objective>
Wire all 7 React pages to fetch real data from the FastAPI backend instead of using hardcoded mock data. Add loading and error states.

Purpose: Connect the frontend and backend so the React app displays real specialist, event, pipeline, and calendar data from the Python data files.

Output: All 7 pages fetch from `/api/*` endpoints, display real data, handle loading/error gracefully.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-fastapi-backend-react-promotion/08.5-01-SUMMARY.md
@.planning/phases/08.5-fastapi-backend-react-promotion/08.5-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 api.ts -->
From frontend/src/lib/api.ts:
```typescript
export const fetchSpecialists: () => Promise<Specialist[]>
export const fetchEvents: () => Promise<CppEvent[]>
export const fetchPipeline: () => Promise<PipelineRecord[]>
export const fetchCalendar: () => Promise<CalendarEvent[]>
export const fetchContacts: () => Promise<PocContact[]>
export const rankSpeakers: (eventName: string, limit?: number) => Promise<RankedMatch[]>
export const scoreSpeaker: (speakerName: string, eventName: string) => Promise<MatchScore>
export const generateEmail: (speakerName: string, eventName: string) => Promise<{email: string}>
export const generateIcs: (eventName: string, eventDate?: string, location?: string, description?: string) => Promise<{ics_content: string}>
```

Specialist shape: { name, board_role, metro_region, company, title, expertise_tags, initials }
PipelineRecord shape: { event_name, speaker_name, match_score, rank, stage, stage_order }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Volunteers and Pipeline pages to real API data</name>
  <files>
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Volunteers.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Pipeline.tsx
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Volunteers.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Pipeline.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/lib/api.ts,
    Category 3 - IA West Smart Match CRM/data/data_speaker_profiles.csv,
    Category 3 - IA West Smart Match CRM/data/pipeline_sample_data.csv
  </read_first>
  <action>
    **Volunteers.tsx:**

    1. Read the current file completely. It has hardcoded `const volunteers = [...]` array.
    2. Replace the hardcoded array with a `useEffect` + `useState` pattern:
       - `import { useState, useEffect } from "react";`
       - `import { fetchSpecialists, type Specialist } from "@/lib/api";`
       - State: `const [volunteers, setVolunteers] = useState<Specialist[]>([]);`
       - State: `const [loading, setLoading] = useState(true);`
       - State: `const [error, setError] = useState<string | null>(null);`
       - useEffect on mount: call `fetchSpecialists()`, set state, catch errors, finally set loading=false
    3. Map the Specialist fields to the existing UI card structure:
       - `name` -> volunteer name
       - `title` -> role display
       - `company` -> company display
       - `metro_region` -> location display
       - `expertise_tags` -> split by `;` or `,` into expertise array
       - `initials` -> avatar initials
       - `board_role` -> badge/role indicator
    4. Add a loading skeleton: when `loading` is true, show 6 placeholder cards with `animate-pulse` class on a `bg-gray-200 rounded` div.
    5. Add error state: when `error` is not null, show a centered message with red text.
    6. Keep ALL existing visual design, layout classes, icons, search/filter UI. Only change the data source.
    7. Remove fields that don't exist in real data (like `pastEngagements`, `matchScoreAvg`, `available`) — either remove those UI elements or show "N/A".

    **Pipeline.tsx:**

    1. Read the current file completely. It has hardcoded `const pipelineData = [...]`, `conversionRates`, `universityBreakdown`.
    2. Replace with API fetch:
       - `import { fetchPipeline, type PipelineRecord } from "@/lib/api";`
       - State: `const [pipelineRecords, setPipelineRecords] = useState<PipelineRecord[]>([]);`
       - State: `const [loading, setLoading] = useState(true);`
       - State: `const [error, setError] = useState<string | null>(null);`
       - useEffect on mount: call `fetchPipeline()`, set state
    3. Transform the flat pipeline records into the funnel chart format the existing Recharts FunnelChart expects:
       - Group records by `stage` field
       - Count records per stage
       - Map to `{ stage: string, count: number, fill: string }` array
       - Use the stage order from `stage_order` field to sort
       - Assign colors from the existing hardcoded palette: `["#a78bfa", "#8b5cf6", "#7c3aed", "#6d28d9", "#5b21b6"]`
    4. For conversionRates: compute from the stage counts (stage N+1 count / stage N count * 100)
    5. For universityBreakdown: if the pipeline data doesn't have university info, keep the existing mock data as placeholder or remove that section. Check the CSV columns first.
    6. Add loading and error states same as Volunteers.
    7. Keep ALL existing visual design, layout, Recharts components, filter UI.

    Run: `cd "Category 3 - IA West Smart Match CRM/frontend" && npx vite build` to verify compilation.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM/frontend" && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Volunteers.tsx contains `import { fetchSpecialists` or `import { fetchSpecialists,`
    - Volunteers.tsx contains `useEffect(`
    - Volunteers.tsx contains `useState<Specialist[]>`
    - Volunteers.tsx does NOT contain the old hardcoded `const volunteers = [` with `id: 1, name: "Sarah Chen"`
    - Pipeline.tsx contains `import { fetchPipeline` or `import { fetchPipeline,`
    - Pipeline.tsx contains `useEffect(`
    - Pipeline.tsx contains `useState<PipelineRecord[]>`
    - Pipeline.tsx does NOT contain the old hardcoded `const pipelineData = [` with `stage: "Campus Engagement"`
    - `npx vite build` exits 0 in frontend/
  </acceptance_criteria>
  <done>Volunteers page fetches real specialists from API and renders in existing card grid. Pipeline page fetches real pipeline data and renders in existing funnel chart. Both have loading and error states. Build compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire remaining 5 pages to API data</name>
  <files>
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Dashboard.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Calendar.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Outreach.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Opportunities.tsx
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Dashboard.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Calendar.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Outreach.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Opportunities.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/lib/api.ts,
    Category 3 - IA West Smart Match CRM/data/data_event_calendar.csv,
    Category 3 - IA West Smart Match CRM/data/data_cpp_events_contacts.csv
  </read_first>
  <action>
    For each page, follow the same pattern: read current file, replace hardcoded data arrays with useEffect+useState fetch from API, add loading/error states, map API response fields to existing UI component props.

    **Dashboard.tsx:**
    - Fetch specialists, events, and pipeline data (3 parallel fetches using Promise.all)
    - `import { fetchSpecialists, fetchEvents, fetchPipeline } from "@/lib/api";`
    - Derive summary metrics: total specialists count, total events count, pipeline stage counts
    - Replace any hardcoded metric numbers with computed values
    - Keep all existing card layouts, charts, visual design

    **AIMatching.tsx:**
    - This page likely has a form/selector to pick an event, then shows matches
    - `import { fetchEvents, rankSpeakers, type CppEvent, type RankedMatch } from "@/lib/api";`
    - Fetch events list on mount for the event selector dropdown
    - On event selection, call `rankSpeakers(selectedEventName)` and display results
    - If the page has hardcoded match results, replace with the API response
    - Add loading spinner during ranking API call
    - Map RankedMatch fields (name, score, rank) to existing card/table UI

    **Calendar.tsx:**
    - `import { fetchCalendar, type CalendarEvent } from "@/lib/api";`
    - Fetch calendar events on mount
    - Map API response fields to the existing calendar display (check CSV columns: likely has date, event name, location)
    - Replace hardcoded events array

    **Outreach.tsx:**
    - This page likely shows outreach email generation
    - `import { fetchEvents, fetchSpecialists, generateEmail, generateIcs } from "@/lib/api";`
    - Fetch events and specialists for dropdowns
    - On "generate" action, call `generateEmail(speakerName, eventName)` and display result
    - Wire ICS download button to `generateIcs()`
    - Replace hardcoded content

    **Opportunities.tsx:**
    - `import { fetchEvents, type CppEvent } from "@/lib/api";`
    - Fetch events on mount
    - Map CPP event fields to opportunity cards/list items
    - Replace hardcoded opportunities array

    For ALL pages:
    - Add `const [loading, setLoading] = useState(true);` and `const [error, setError] = useState<string | null>(null);`
    - Show loading skeleton with `animate-pulse` when loading
    - Show error message when error is not null
    - Keep ALL existing visual design, layout classes, icons

    Run: `cd "Category 3 - IA West Smart Match CRM/frontend" && npx vite build` to verify compilation.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM/frontend" && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx contains `import {` with at least one of `fetchSpecialists`, `fetchEvents`, `fetchPipeline`
    - Dashboard.tsx contains `useEffect(`
    - AIMatching.tsx contains `import {` with `rankSpeakers`
    - AIMatching.tsx contains `useEffect(`
    - Calendar.tsx contains `import {` with `fetchCalendar`
    - Calendar.tsx contains `useEffect(`
    - Outreach.tsx contains `import {` with at least one of `generateEmail`, `generateIcs`
    - Opportunities.tsx contains `import {` with `fetchEvents`
    - Opportunities.tsx contains `useEffect(`
    - `npx vite build` exits 0 in frontend/
  </acceptance_criteria>
  <done>All 5 remaining pages wire to real API data. Dashboard shows derived metrics, AI Matching triggers ranking, Calendar shows real events, Outreach generates emails/ICS, Opportunities shows real events. All have loading/error states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of full stack integration</name>
  <files>None (verification only)</files>
  <action>Human verifies all 7 React pages render with real data from FastAPI backend.</action>
  <verify>Human visual inspection confirms real data renders on all pages.</verify>
  <done>All pages show real data, no CORS errors, human approved.</done>
  <what-built>
    Full FastAPI + React stack: 9 API endpoints wrapping existing Python logic, React frontend promoted from V1.1 mockup with all 7 pages wired to fetch real data from the API.
  </what-built>
  <how-to-verify>
    1. Start the FastAPI backend:
       ```bash
       cd "Category 3 - IA West Smart Match CRM"
       python -m uvicorn src.api.main:app --reload --port 8000
       ```
    2. In a separate terminal, start the React dev server:
       ```bash
       cd "Category 3 - IA West Smart Match CRM/frontend"
       npm run dev
       ```
    3. Open http://localhost:5173 in browser
    4. Verify Dashboard page loads with real metric numbers (not zeros or placeholders)
    5. Navigate to Volunteers — verify specialist cards show real names (e.g., from data_speaker_profiles.csv)
    6. Navigate to Pipeline — verify funnel chart renders with real stage data
    7. Navigate to AI Matching — select an event and verify ranked matches appear
    8. Navigate to Calendar — verify events display
    9. Navigate to Opportunities — verify event listings
    10. Navigate to Outreach — verify the page loads
    11. Check browser console for no CORS errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd "Category 3 - IA West Smart Match CRM"
# Backend tests pass
python -m pytest tests/test_api_data.py tests/test_api_matching.py -v
# Frontend builds
cd frontend && npx vite build
# All page files import from api.ts
grep -l "from.*lib/api" src/app/pages/*.tsx | wc -l
# Expected: 7 (all pages)
```
</verification>

<success_criteria>
- All 7 React pages import from `@/lib/api` and use useEffect to fetch data
- No hardcoded mock data arrays remain in page files
- `npx vite build` succeeds
- Visual verification: Volunteers grid shows real specialists, Pipeline funnel shows real data
- Human approves the visual/functional result
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-fastapi-backend-react-promotion/08.5-03-SUMMARY.md`
</output>
