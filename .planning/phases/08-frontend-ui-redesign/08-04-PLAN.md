---
phase: 08-frontend-ui-redesign
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"
autonomous: true
requirements: [REQ-UI-03]
must_haves:
  truths:
    - "Login page shows two role selection cards side by side: Coordinator and Volunteer"
    - "Coordinator card has a 'Demo Login' button that sets user_role to 'coordinator' and navigates to coordinator dashboard"
    - "Volunteer card shows 'Connect with LinkedIn' button that is non-functional and displays 'Coming Soon' badge"
    - "Login page hides Streamlit sidebar but shows a minimal glass nav bar"
    - "Login page renders via st.components.v1.html() with Academic Curator design system"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"
      provides: "Login/role-selection page"
      exports: ["render_login_page"]
      min_lines: 80
  key_links:
    - from: "src/ui/login_page.py"
      to: "src/ui/html_base.py"
      via: "render_html_page()"
      pattern: "from src.ui.html_base import"
    - from: "src/ui/login_page.py"
      to: "src/ui/page_router.py"
      via: "navigate_to(), set_user_role()"
      pattern: "from src.ui.page_router import"
---

<objective>
Build the login/role-selection page. Two cards side by side: Coordinator (functional demo login) and Volunteer (placeholder with LinkedIn Coming Soon). Uses the Academic Curator design system via st.components.v1.html().

Purpose: This is the gate between the public landing page and the authenticated coordinator dashboard. For the hackathon, coordinator login is a simple demo button; volunteer login is placeholder only (per deferred decisions).

Output: `src/ui/login_page.py` with `render_login_page()` function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-ui-redesign/08-CONTEXT.md

<interfaces>
From src/ui/html_base.py (Plan 01):
```python
def render_html_page(body: str, *, title: str = "IA SmartMatch", height: int = 4000, hide_chrome: bool = False) -> None: ...
```

From src/ui/page_router.py (Plan 01):
```python
def navigate_to(page: str) -> None: ...
def set_user_role(role: str) -> None: ...
```

From src/ui/design_system.py (Plan 01):
```python
PRIMARY: str = "#005394"
PRIMARY_CONTAINER: str = "#2b6cb0"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login_page.py with role selection UI</name>
  <files>Category 3 - IA West Smart Match CRM/src/ui/login_page.py</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/src/ui/html_base.py (render_html_page API)
    - Category 3 - IA West Smart Match CRM/src/ui/page_router.py (navigate_to, set_user_role)
    - Category 3 - IA West Smart Match CRM/src/ui/design_system.py (color constants)
    - Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/ia_smartmatch_landing_page_updated/code.html (nav bar style reference, lines 89-104)
  </read_first>
  <action>
Create `src/ui/login_page.py` with `render_login_page() -> None`:

1. Build an HTML body string with these sections following the Academic Curator design system:

**Glass nav bar** (fixed top, same style as landing page):
```html
<nav class="bg-white/80 backdrop-blur-xl shadow-sm fixed top-0 w-full z-50 flex justify-between items-center px-8 py-4">
  <span class="text-xl font-bold tracking-tight text-on-surface font-headline">IA SmartMatch</span>
  <a href="#" class="text-primary font-semibold text-sm">Back to Home</a>
</nav>
```

**Main content** (centered, vertically centered using min-h-screen flex):
```html
<main class="pt-24 min-h-screen flex flex-col items-center justify-center bg-surface px-8">
  <h1 class="text-4xl font-headline font-bold text-on-surface mb-2">Welcome Back</h1>
  <p class="text-on-surface-variant mb-12 text-lg">Select your role to continue</p>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-3xl w-full">
    <!-- Coordinator Card -->
    <div class="bg-surface-container-lowest rounded-[24px] p-8 shadow-sm hover:shadow-md transition-shadow">
      <div class="w-14 h-14 rounded-2xl bg-primary flex items-center justify-center text-white mb-6">
        <span class="material-symbols-outlined text-3xl">school</span>
      </div>
      <h2 class="text-2xl font-headline font-bold text-on-surface mb-2">Coordinator</h2>
      <p class="text-on-surface-variant mb-6">Access the institutional dashboard, manage matches, and oversee the specialist engagement pipeline.</p>
      <div class="space-y-3">
        <button id="demo-login-btn" class="w-full py-3 hero-gradient text-white rounded-xl font-semibold text-sm hover:opacity-90 transition-all">
          Demo Login
        </button>
        <p class="text-xs text-on-surface-variant text-center">No credentials needed for demo</p>
      </div>
    </div>
    <!-- Volunteer Card -->
    <div class="bg-surface-container-lowest rounded-[24px] p-8 shadow-sm relative overflow-hidden opacity-80">
      <div class="absolute top-4 right-4 px-3 py-1 bg-tertiary-fixed text-on-tertiary-fixed rounded-full text-[10px] font-bold uppercase">Coming Soon</div>
      <div class="w-14 h-14 rounded-2xl bg-secondary flex items-center justify-center text-white mb-6">
        <span class="material-symbols-outlined text-3xl">person</span>
      </div>
      <h2 class="text-2xl font-headline font-bold text-on-surface mb-2">Volunteer</h2>
      <p class="text-on-surface-variant mb-6">View your speaking assignments, update availability, and connect with engagement opportunities.</p>
      <button class="w-full py-3 bg-surface-container-high text-on-surface-variant rounded-xl font-semibold text-sm cursor-not-allowed" disabled>
        Connect with LinkedIn
      </button>
      <p class="text-xs text-on-surface-variant text-center mt-3">LinkedIn OAuth integration coming soon</p>
    </div>
  </div>
</main>
```

2. Call `render_html_page(body_html, title="IA SmartMatch | Login", height=900, hide_chrome=True)`

3. After the HTML component, render Streamlit buttons below for actual navigation:
```python
st.markdown("---")
col1, col2, col3 = st.columns([1, 1, 1])
with col1:
    if st.button("< Back to Home", use_container_width=True):
        navigate_to("landing")
with col2:
    if st.button("Coordinator Demo Login", type="primary", use_container_width=True):
        set_user_role("coordinator")
        navigate_to("coordinator")
with col3:
    st.button("Volunteer (Coming Soon)", disabled=True, use_container_width=True)
```

Import `navigate_to`, `set_user_role` from `src.ui.page_router` and `render_html_page` from `src.ui.html_base`.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/DangT/Documents/GitHub/HackathonForBetterFuture2026" && python -c "
import inspect
from src.ui.login_page import render_login_page
src = inspect.getsource(render_login_page)
assert 'Coordinator' in src
assert 'Volunteer' in src
assert 'Coming Soon' in src
assert 'Connect with LinkedIn' in src
assert 'render_html_page' in src or 'wrap_html' in src
assert 'navigate_to' in src
assert 'set_user_role' in src
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/login_page.py`
    - `grep -c "render_login_page" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - `grep -c "Coordinator" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - `grep -c "Volunteer" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - `grep -c "Coming Soon" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - `grep -c "navigate_to" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - `grep -c "set_user_role" "Category 3 - IA West Smart Match CRM/src/ui/login_page.py"` returns >= 1
    - Module imports without error: `python -c "from src.ui.login_page import render_login_page"`
  </acceptance_criteria>
  <done>Login page renders two role cards via st.components.v1.html(). Coordinator card has working demo login button. Volunteer card shows "Coming Soon" badge and disabled LinkedIn button. Streamlit chrome hidden. Navigation wired via page_router.</done>
</task>

</tasks>

<verification>
- Login page shows Coordinator and Volunteer cards
- Coordinator demo login navigates to coordinator dashboard
- Volunteer card is visually disabled with "Coming Soon" badge
- Page uses Academic Curator design system (glass nav, rounded cards, hero-gradient buttons)
</verification>

<success_criteria>
`render_login_page()` renders a role-selection UI matching the Academic Curator design system. Coordinator demo login works. Volunteer login is placeholder-only.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-ui-redesign/08-04-SUMMARY.md`
</output>
