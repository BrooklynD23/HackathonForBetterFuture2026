---
phase: 08-frontend-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"
  - "Category 3 - IA West Smart Match CRM/src/ui/page_router.py"
  - "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"
autonomous: true
requirements: [REQ-UI-01]
must_haves:
  truths:
    - "A shared design system module exports the full Tailwind config, CSS, and font links as reusable Python strings"
    - "A page router manages session_state navigation between landing, login, coordinator, and match_engine pages"
    - "An html_base helper wraps any HTML body string into a full standalone document with Tailwind CDN, fonts, Material Symbols, and the Academic Curator Tailwind config"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"
      provides: "Tailwind config JS, shared CSS, font links, color constants"
      contains: "TAILWIND_CONFIG"
    - path: "Category 3 - IA West Smart Match CRM/src/ui/page_router.py"
      provides: "Page routing via session_state"
      exports: ["navigate_to", "get_current_page", "init_page_state"]
    - path: "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"
      provides: "HTML document wrapper for st.components.v1.html()"
      exports: ["wrap_html"]
  key_links:
    - from: "src/ui/html_base.py"
      to: "src/ui/design_system.py"
      via: "imports TAILWIND_CONFIG, SHARED_CSS, FONT_LINKS"
      pattern: "from src.ui.design_system import"
---

<objective>
Create the shared design system foundation, page routing, and HTML base wrapper that all subsequent UI pages depend on.

Purpose: Every page in Phase 8 renders via `st.components.v1.html()` with Tailwind CSS from CDN. This plan establishes the shared infrastructure so pages can be built independently in Wave 2/3 without duplicating boilerplate.

Output: Three new Python modules in `src/ui/` — `design_system.py`, `page_router.py`, `html_base.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-ui-redesign/08-CONTEXT.md
@Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/academic_blueprints/DESIGN.md
@Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/ia_smartmatch_landing_page_updated/code.html (reference for Tailwind config object)
@Category 3 - IA West Smart Match CRM/src/ui/styles.py (existing design tokens — preserve for backward compat)
@Category 3 - IA West Smart Match CRM/src/app.py (current routing via session_state current_view)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design_system.py with Tailwind config and shared CSS</name>
  <files>Category 3 - IA West Smart Match CRM/src/ui/design_system.py</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/ia_smartmatch_landing_page_updated/code.html (lines 10-86 contain the exact Tailwind config and CSS)
    - Category 3 - IA West Smart Match CRM/docs/mockup/V1.0/stitch_relo_ai_native_crm_landing_page/academic_blueprints/DESIGN.md
    - Category 3 - IA West Smart Match CRM/src/ui/styles.py (existing tokens to preserve)
  </read_first>
  <action>
Create `src/ui/design_system.py` with the following constants (all as Python strings):

1. **FONT_LINKS** — the three Google Fonts link tags from the mockup:
   - `https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Inter+Tight:wght@600;700;800&display=swap`
   - `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap`

2. **TAILWIND_CDN** — `<script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>`

3. **TAILWIND_CONFIG** — the exact `<script>tailwind.config = {...}</script>` block from the landing page mockup lines 10-73. Copy the full color map, fontFamily, borderRadius verbatim. This must be a single Python string containing the full script tag.

4. **SHARED_CSS** — a `<style>` block containing:
   - `.material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }`
   - `.hero-gradient { background: linear-gradient(135deg, #005394 0%, #2b6cb0 100%); }`
   - `.glass-panel { background: rgba(255, 255, 255, 0.8); backdrop-filter: blur(24px); }`
   - `.bg-primary-gradient { background: linear-gradient(135deg, #005394 0%, #2b6cb0 100%); }`

5. **HIDE_STREAMLIT_CHROME** — a `<style>` string that hides Streamlit's header, footer, sidebar toggle, and main menu:
   ```css
   #MainMenu {visibility: hidden;}
   footer {visibility: hidden;}
   header[data-testid="stHeader"] {display: none;}
   section[data-testid="stSidebar"] {display: none;}
   div[data-testid="stSidebarCollapsedControl"] {display: none;}
   .stDeployButton {display: none;}
   ```

6. **Color constants** as Python strings: `PRIMARY = "#005394"`, `PRIMARY_CONTAINER = "#2b6cb0"`, `ON_PRIMARY = "#ffffff"`, `ON_SURFACE = "#191c1e"`, `ON_SURFACE_VARIANT = "#414750"`, `SURFACE = "#f7f9fc"`, `SURFACE_CONTAINER_LOW = "#f2f4f7"`, `SURFACE_CONTAINER = "#eceef1"`, `SURFACE_CONTAINER_LOWEST = "#ffffff"`.

Add type annotations on all constants (`: str`). Add module docstring referencing the Academic Curator design system.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/DangT/Documents/GitHub/HackathonForBetterFuture2026" && python -c "from src.ui.design_system import TAILWIND_CONFIG, SHARED_CSS, FONT_LINKS, TAILWIND_CDN, HIDE_STREAMLIT_CHROME, PRIMARY; assert '#005394' in TAILWIND_CONFIG; assert 'hero-gradient' in SHARED_CSS; assert 'Inter+Tight' in FONT_LINKS; assert 'tailwindcss.com' in TAILWIND_CDN; assert 'stHeader' in HIDE_STREAMLIT_CHROME; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/design_system.py`
    - `grep -c "TAILWIND_CONFIG" "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"` returns >= 1
    - `grep -c "HIDE_STREAMLIT_CHROME" "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"` returns >= 1
    - `grep -c "hero-gradient" "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"` returns >= 1
    - `grep "005394" "Category 3 - IA West Smart Match CRM/src/ui/design_system.py"` matches in both TAILWIND_CONFIG and PRIMARY constant
    - Module imports successfully: `python -c "from src.ui.design_system import TAILWIND_CONFIG"`
  </acceptance_criteria>
  <done>design_system.py exports TAILWIND_CONFIG, TAILWIND_CDN, FONT_LINKS, SHARED_CSS, HIDE_STREAMLIT_CHROME, and all color constants. The Tailwind config matches the mockup's color palette exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create html_base.py wrapper and page_router.py</name>
  <files>Category 3 - IA West Smart Match CRM/src/ui/html_base.py, Category 3 - IA West Smart Match CRM/src/ui/page_router.py</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/src/ui/design_system.py (just created in Task 1)
    - Category 3 - IA West Smart Match CRM/src/app.py (current session_state routing pattern)
  </read_first>
  <action>
**html_base.py:**

Create a `wrap_html(body: str, *, title: str = "IA SmartMatch", height: int = 2000, hide_chrome: bool = False) -> None` function that:
1. Constructs a full HTML document string: `<!DOCTYPE html><html lang="en"><head>` + FONT_LINKS + TAILWIND_CDN + TAILWIND_CONFIG + SHARED_CSS + `</head><body class="bg-surface font-body text-on-surface antialiased">` + body + `</body></html>`
2. If `hide_chrome=True`, also calls `st.markdown(HIDE_STREAMLIT_CHROME, unsafe_allow_html=True)` before the component
3. Calls `st.components.v1.html(html_string, height=height, scrolling=True)`

Also create `render_html_page(body: str, *, title: str = "IA SmartMatch", height: int = 4000, hide_chrome: bool = False) -> None` as an alias that defaults to height=4000 (for full pages).

Import from `src.ui.design_system`: FONT_LINKS, TAILWIND_CDN, TAILWIND_CONFIG, SHARED_CSS, HIDE_STREAMLIT_CHROME.

**page_router.py:**

Create a module with:
- `PAGES` tuple: `("landing", "login", "coordinator", "match_engine")`
- `def init_page_state() -> None:` — sets `st.session_state["current_page"]` to `"landing"` if not present; also sets `st.session_state["user_role"]` to `None` if not present
- `def get_current_page() -> str:` — returns `st.session_state.get("current_page", "landing")`
- `def navigate_to(page: str) -> None:` — sets `st.session_state["current_page"] = page` and calls `st.rerun()`
- `def set_user_role(role: str) -> None:` — sets `st.session_state["user_role"] = role`
- `def get_user_role() -> str | None:` — returns `st.session_state.get("user_role")`
- `def is_authenticated() -> bool:` — returns `st.session_state.get("user_role") is not None`

All functions have type annotations.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/DangT/Documents/GitHub/HackathonForBetterFuture2026" && python -c "
from src.ui.html_base import wrap_html, render_html_page
from src.ui.page_router import init_page_state, get_current_page, navigate_to, set_user_role, get_user_role, is_authenticated, PAGES
assert 'landing' in PAGES
assert 'coordinator' in PAGES
assert 'match_engine' in PAGES
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wrap_html" "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"` returns >= 1
    - `grep -c "render_html_page" "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"` returns >= 1
    - `grep -c "st.components.v1.html" "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"` returns >= 1
    - `grep -c "from src.ui.design_system import" "Category 3 - IA West Smart Match CRM/src/ui/html_base.py"` returns >= 1
    - `grep -c "navigate_to" "Category 3 - IA West Smart Match CRM/src/ui/page_router.py"` returns >= 1
    - `grep -c "current_page" "Category 3 - IA West Smart Match CRM/src/ui/page_router.py"` returns >= 1
    - Both modules import successfully
  </acceptance_criteria>
  <done>html_base.py wraps HTML body strings into complete documents with Tailwind CDN and renders via st.components.v1.html(). page_router.py manages session_state navigation between all 4 pages with role tracking.</done>
</task>

</tasks>

<verification>
- All three new modules import without errors
- design_system.py contains the exact Tailwind color palette from the mockup
- html_base.py uses st.components.v1.html() for rendering
- page_router.py supports navigation to landing, login, coordinator, match_engine
</verification>

<success_criteria>
Foundation modules exist and are importable. Subsequent plans (03-05) can import `wrap_html`/`render_html_page` to render full pages and use `navigate_to`/`get_current_page` for routing.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-ui-redesign/08-01-SUMMARY.md`
</output>
